# pnpm: use a flat node_modules like npm so packages that rely on hoisting
# (next plugins, transformers, etc.) resolve exactly as they did under npm.
node-linker=hoisted
strict-peer-dependencies=false
