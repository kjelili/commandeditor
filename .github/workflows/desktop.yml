# CommandEditor Desktop release pipeline
#
# Triggers:
#   - tag push `desktop-v*`  → full matrix build + GitHub Release with installers
#   - workflow_dispatch      → full matrix build, artifacts only (no release)
#
# Each job: builds the web app as a static export (TAURI_BUILD=1 → out/),
# then lets tauri-action compile the Rust shell and bundle native installers
# (.AppImage/.deb on Linux, .msi/.exe on Windows, .dmg on macOS).
name: desktop-release

on:
  workflow_dispatch:
  push:
    tags:
      - 'desktop-v*'

# GITHUB_TOKEN defaults to read-only; creating a GitHub Release needs write.
permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      # pnpm BEFORE setup-node: setup-node v5's package-manager-cache detects
      # "packageManager": "pnpm@..." in package.json and shells out to pnpm —
      # it must already exist on PATH.
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm

      - name: Install web dependencies
        # --ignore-scripts: sharp's postinstall build is unnecessary
        # (images.unoptimized; webpack aliases sharp away)
        run: pnpm install --ignore-scripts

      - name: Build web app (static export for Tauri)
        run: pnpm build
        env:
          TAURI_BUILD: '1'
          # Optional cloud-import OAuth client IDs. Missing secrets resolve to
          # empty strings — lib/cloudConfig.ts then hides those providers in
          # the UI, so the build stays correct either way.
          NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_GOOGLE_CLIENT_ID }}
          NEXT_PUBLIC_GOOGLE_API_KEY: ${{ secrets.NEXT_PUBLIC_GOOGLE_API_KEY }}
          NEXT_PUBLIC_DROPBOX_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_DROPBOX_CLIENT_ID }}
          NEXT_PUBLIC_ONEDRIVE_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_ONEDRIVE_CLIENT_ID }}

      - name: Install desktop npm dependencies (Tauri CLI)
        # tauri-action invokes `npm run tauri build` inside desktop/ — the
        # @tauri-apps/cli devDependency must be installed first (exit 127 fix).
        run: cd desktop && npm install

      - uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: desktop/src-tauri

      - name: Install Linux system dependencies
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.0-dev libgtk-3-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Build desktop app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: desktop
          # Release inputs only on tag pushes — an empty tagName tells
          # tauri-action to skip release creation (manual runs = artifacts only).
          tagName: ${{ startsWith(github.ref, 'refs/tags/') && 'desktop-v__VERSION__' || '' }}
          releaseName: ${{ startsWith(github.ref, 'refs/tags/') && 'CommandEditor Desktop v__VERSION__' || '' }}
          releaseBody: ${{ startsWith(github.ref, 'refs/tags/') && 'Native desktop build of CommandEditor — the private, in-browser PDF toolkit, now as an offline app.' || '' }}
          releaseDraft: true
          prerelease: false

      - name: Upload bundles (manual runs)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: commandeditor-desktop-${{ matrix.os }}
          path: |
            desktop/src-tauri/target/release/bundle/**
          if-no-files-found: warn
